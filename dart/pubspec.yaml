name: mobility
description: Public Transport API in Dart
version: 1.0.0
repository: https://github.com/vitzapp/mobility

environment:
  sdk: ^3.0.3

# Add regular dependencies here.
dependencies:
  http: ^1.0.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
